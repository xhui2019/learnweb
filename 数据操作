#数据查找：ds = data.objects.filter(id__in=[1,3]) 查找符合条件的数据集合
# data.objects.exclude():查询不包含条件的数据集合
# data.objects.get() :查询符合条件的数据，此条件需要时存在且唯一的
# .value() 用于查找数据后返回列表形式

更新模型数据
方法一 ：1、先通过get()方法获取符合条件的数据 2、再通过对象属性赋值进行更新
方法二： 通过update()更新单个或者多个数据行
